export const initialState = {
  favoritePhotosIds: [],
}

export const AppReducer = (state, action) => {
  switch (action.type) {
    case null: {
      return state
    }
    default: {
      return state
    }
  }
}
